@model EducationSystemKZ_Azure.Models.TuitionFee

@{
    ViewBag.Title = "Check";

    if (Session["userID"] == null)
    {
        Response.Redirect("~/Login/Index");
    }
}

<h2>Check</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>TuitionFee</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="form-group">
            @Html.LabelFor(model => model.FeePeriod, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10 input-container">
                @Html.EditorFor(model => model.FeePeriod, new { htmlAttributes = new { @class = "form-control", type = "month" } })
                <span class="input-description">- Choose a month to check which students have not paid by the month and earlier. </span>
                @Html.ValidationMessageFor(model => model.FeePeriod, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Search" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to TuitionFee Page", "Index")
</div>

